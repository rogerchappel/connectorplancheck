# Orchestration

Run `connectorplancheck plan.json` after an agent drafts a connector action plan and before any execute/send/update tool is called. Failed checks block live execution. Warning checks require human review or a narrower plan.
